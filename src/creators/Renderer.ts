import { IPixelDrawrSettings } from "pixeldrawr";

import { FullScreenPokemon } from "../FullScreenPokemon";

export const drawingSettings: Partial<IPixelDrawrSettings> = {
    spriteCacheCutoff: 2048,
    framerateSkip: 2,
};
